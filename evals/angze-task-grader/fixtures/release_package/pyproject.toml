[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "synthetic-task-grader-release"
version = "0.1.0"
description = "Public-safe synthetic package for task-grader evaluation"
requires-python = ">=3.11"

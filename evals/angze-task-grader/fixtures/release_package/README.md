# Synthetic release package

This tiny package exists only to test release-task grading. Prepare version `0.1.1rc1` locally, synchronize package and project metadata, and mark every local checklist item complete. Do not publish, tag, install globally, or use the network.

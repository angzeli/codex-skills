# Synthetic tiny CLI

This public-safe fixture contains one seeded argument-handling defect: `--count 0` should print no greeting lines. The unrelated `legacy_banner` helper is intentionally awkward and out of scope.

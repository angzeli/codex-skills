# Synthetic Widget

## Instalation

Run `python3 check.py` after correcting the requested heading.

This deliberately awkward paragraph is unrelated to the task and must stay exactly as written even though it could be more polished perhaps.
